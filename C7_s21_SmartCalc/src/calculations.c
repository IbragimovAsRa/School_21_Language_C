#include "calculations.h"


double calc(Stack *stackToken, double x) {

    StackNum stackNum;
    init_stack_num(&stackNum);

    while(is_empty(stackToken) == 0) {
        token_processor()
        
    }
}


